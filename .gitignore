node_modules/
dist/
.wrangler/
firebase-debug.log
firestore-debug.log

# ─── Cloudflare / yerli sirlər (AUDIT-TASK-2 / 2.0) ───
# `.dev.vars` JWT_SECRET və TURNSTILE_SECRET daşıyır. `.env*` hazırda mövcud
# deyil, amma qabaqcadan bağlanır — ilk commit-dən sonra təsadüfən yaradılsa
# sükutla izlənməyə başlayardı.
.dev.vars
.dev.vars.*
.env
.env.*
!.env.example

# ─── Miqrasiya artefaktları (AUDIT-TASK-1-REPORT §5.4 → AUDIT-TASK-5 §5.7) ───
# Firebase→D1 miqrasiya qovluğu (`migration-cf/`) 2026-07-27-də TAMAMİLƏ
# SİLİNDİ: `import.sql` (53 istifadəçi + post/DM/mesaj), `update.sql`
# (R2 yolları) və generator script-ləri. Miqrasiya artıq lazım deyil —
# istehsalda yalnız sahibin öz hesabı saxlanılır (istifadəçi qərarı).
# Naxış qalır ki, artefakt təsadüfən yenidən yaradılsa git-ə düşməsin.
migration-cf/

# Playwright (E2E)
/test-results/
/playwright-report/
/blob-report/
/.playwright/

# E2E sessiya faylı (giriş cookie-si)
/e2e/.auth/

# ─── Sirlər: heç bir halda commit edilməməlidir (AUDIT-2026-07-26 / M-16) ───
# ⚠ Git YALNIZ forward slash tanıyır. Backslash ilə yazılan qayda SÜKUTLA
# işləmir — burada əvvəl `testsprite_tests\tmp\config.json` sətri var idi və
# məhz buna görə API açarı faylı heç vaxt ignore edilməmişdi. Səhv yalnız
# auditdə üzə çıxdı, çünki Git belə qaydaya xəta vermir.
#
# Eyni səbəbdən köhnə `migration/serviceAccountKey.json` qaydası da çıxarıldı:
# o, yalnız KÖK `migration/` qovluğuna şamil olunurdu və faktiki yol olan
# `legacy/firebase/migration/`-i tutmurdu. `**/` ilə hər dərinlik tutulur.
**/serviceAccountKey.json
**/.emulator-data/
**/auth_export/
**/*.pem
**/*.key
testsprite_tests/

# ─── Köhnəlmiş / artefakt (AUDIT-2026-07-26 / struktur borcu #3) ───
legacy/
.claude/skills/run-collabix/shots/


.vscode
.vs

# Əl ilə çıxarılan git log dump-ları (repoya aid deyil)
commits_*.txt

# Responsive audit hesabatları (generasiya olunur, mənbə deyil)
audit-reports/
git_history_full.txt
git_history_short.txt
CLAUDE.md
AUDİT/COLLABIX BACKEND — FULL-SCALE ENGINEERING AUDIT.md
