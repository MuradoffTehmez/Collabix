import { Env } from '../util';
import { EventPayload } from '../events';

export async function processCertificateWorkflow(_env: Env, _event: EventPayload<any>): Promise<void> {
  // Placeholder for certificate workflow
}
